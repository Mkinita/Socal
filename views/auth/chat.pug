extends ./layout/index


block contenido
    
    h2(class="text-2xl font-extrabold text-center my-10")=pagina




    //-pre= JSON.stringify(chats, undefined, 2)
    div(class="mx-auto max-w-4xl bg-white shadow")

        each usuario in usuarios  
                          
                            div(class="p-6 w-full flex flex-col sm:flex-row sm:items-center sm:justify-between  md:space-y-0 gap-4")
                                div(class="sm:w-1/4 md:w-1/6") 
                                   a(class="block text-2xl font-extrabold text-black truncate" href=`/chat/${usuario.id}`)= usuario.nombre
                                   p=usuario.email
                                    



 
        